import { TopHeader } from "../../components/TopHeader";
import { Banner } from "../../components/Banner";

export const HomeView = () => {
    return (
        <main>
            <Banner />
            <TopHeader />
        </main>
    );
};
